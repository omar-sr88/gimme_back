<% provide(:title, 'Sign up') %>
<% provide(:button_text, 'Create my account') %>

<div class="signup-form">
	<div class="row">
	  <div class="col-md-6 col-md-offset-3">
	        <%= render 'form' %>
	  </div>
	</div>
	<div class="log-sign-switch row"><%= link_to "<button>Already a user? Log in</button>".html_safe, login_path, :id => "go-signup" %>
</div>
